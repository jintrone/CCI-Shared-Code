Steps needed to be able to run/compile this project:

1. Compile and install pangaea-core in local mvn repository: 
  mvn package install:install-file -DgroupId=edu.mit.cci.simulation -DartifactId=pangaea-core -Dversion=0.1.0-SNAPSHOT -Dpackaging=jar -Dfile=target/pangaea-core-0.1.0-SNAPSHOT.jar -DpomFile=pom.xml

2. 