In order to be able to compile / test this project you need to obtain following artefacts:
  - Vensim.jar
  	  use command: mvn install:install-file -DgroupId=Vensim -DartifactId=vensim -Dversion=1.0 -Dpackaging=jar -Dfile=Vensim.jar
  	
  - 